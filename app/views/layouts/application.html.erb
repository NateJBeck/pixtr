<!DOCTYPE html>
<html>
<head>
  <title>Pixtr</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <style>
    body {
      background-color: bisque;
      color:black;
      max-width: 960px;
      font-family:georgia;
      font-family:300%;
      margin: 0 auto;
    }
    p {
      font-family:georgia;
      font-size:160%;
    }
    .session {
      float: right;
    }
    img {
      max-height: 45%; 
      max-width: 45%;
    }
    span {
      color:blue;
      font-size:75%;
    }
  </style>
</head>
<body>
  <header class="session">

  <% if signed_in? %>
    <%= current_user.email %>
    <%= link_to "Sign out", session_path, method: :delete %>
  <% else %>
    <%= link_to "Sign in", new_session_path %>
    <%= link_to "Sign up", new_user_path %>
  <% end %>

  </header>

  <h1><%= link_to "Pixtr", root_path %><span> 
      <img src="http://icons.iconarchive.com/icons/martz90/circle/512/camera-icon.png" height="42" width="42" /> for sharing photos</span></h1>

  <% flash.each do |name, msg| %>
    <% if msg.is_a?(String) %>
      <%= content_tag :div, msg, :class => "flash_#{name}" %>
    <% end %>
  <% end %>

  <%= yield %>
</body>
</html>
