<h2> This is a page for <%= @gallery.name %> </h2>

<%= link_to "Add an Image", new_gallery_image_path(@gallery) %>

<% @gallery.images.each do |image| %> <%#no equals sign for .each %>
  <%= image_tag image.url %>
  <%= link_to image.name, gallery_image_path(@gallery, image) %>
<% end %>
